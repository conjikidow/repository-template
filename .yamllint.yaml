---
extends: default

rules:
  document-start:
    present: true
    ignore: |
      .github/workflows/*.yml
      .github/workflows/*.yaml
  truthy:
    check-keys: false
  comments:
    ignore: |
      .github/workflows/*.yml
      .github/workflows/*.yaml
  line-length:
    max: 120
